[project]
name = "local-voc-archive"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "pydantic-settings>=2,<3",
]

[dependency-groups]
dev = [
  "pytest>=8,<9",
]

[tool.pytest.ini_options]
pythonpath = ["."]
